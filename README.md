# FluffNwoof - نظام إدارة العيادات البيطرية 🐾

نظام متكامل لإدارة العيادات البيطرية يشمل إدارة الحيوانات الأليفة، المواعيد، السجلات الطبية، والفواتير.

## 🚀 التقنيات المستخدمة

### Backend
- **Node.js** مع **Express.js**
- **TypeScript** للـ type safety
- **PostgreSQL** على **Neon** (قاعدة بيانات serverless)
- **Prisma ORM** للتعامل مع قاعدة البيانات
- **JWT** للمصادقة والتفويض
- **bcrypt** لتشفير كلمات المرور

### Frontend
- **React 18** مع **TypeScript**
- **Vite** كـ build tool
- **TailwindCSS** للتصميم
- **React Router v7** للتنقل
- **TanStack Query (React Query)** لإدارة حالة البيانات
- **Zustand** لإدارة الحالة المحلية
- **React Hook Form** للنماذج
- **Axios** للطلبات HTTP

## 📋 المميزات

### ✅ المميزات المنفذة حالياً

1. **نظام المصادقة والتفويض**
   - تسجيل الدخول/الخروج
   - إدارة الجلسات بـ JWT
   - صلاحيات متعددة (Admin, Vet, Receptionist)

2. **إدارة الحيوانات الأليفة والملاك**
   - CRUD كامل للملاك
   - CRUD كامل للحيوانات الأليفة
   - البحث والفلترة
   - ربط الحيوانات بأصحابها

3. **إدارة المواعيد**
   - إنشاء وإدارة المواعيد
   - تتبع حالة الموعد
   - عرض المواعيد القادمة

4. **البنية الأساسية للواجهة**
   - Layout متجاوب
   - Sidebar للتنقل
   - صفحة Dashboard
   - صفحة Login

### 🚧 قيد التطوير

- إدارة السجلات الطبية الكاملة
- إدارة الفواتير والمحاسبة
- نظام التطعيمات
- التقارير والإحصائيات
- رفع الملفات والصور

## 🛠️ البدء في المشروع

### المتطلبات الأساسية

- Node.js (v18 أو أحدث)
- pnpm (أو npm/yarn)
- PostgreSQL database (أو حساب Neon)

### 1️⃣ إعداد Backend

```bash
# الانتقال لمجلد Backend
cd backend

# تثبيت المكتبات
pnpm install

# إنشاء ملف .env من .env.example
cp .env.example .env

# تحديث متغيرات البيئة في .env:
# DATABASE_URL="postgresql://username:password@host:5432/fluffnwoof?sslmode=require"
# JWT_SECRET="your-secret-key-here"

# توليد Prisma Client
pnpm prisma:generate

# تطبيق Migrations على قاعدة البيانات
pnpm prisma:migrate

# تشغيل السيرفر في وضع التطوير
pnpm dev
```

السيرفر سيعمل على: `http://localhost:5000`

### 2️⃣ إعداد Frontend

```bash
# في نافذة terminal جديدة
cd frontend

# تثبيت المكتبات
pnpm install

# تشغيل Frontend في وضع التطوير
pnpm dev
```

Frontend سيعمل على: `http://localhost:5173`

## 📊 قاعدة البيانات

### إعداد قاعدة البيانات على Neon

1. اذهب إلى [Neon](https://neon.tech) وأنشئ حساب مجاني
2. أنشئ مشروع جديد
3. احصل على رابط الاتصال (Connection String)
4. ضع الرابط في ملف `.env` في متغير `DATABASE_URL`

### إدارة قاعدة البيانات

```bash
# عرض قاعدة البيانات في Prisma Studio
cd backend
pnpm prisma:studio

# إنشاء migration جديد
pnpm prisma:migrate

# إعادة بناء قاعدة البيانات (⚠️ سيحذف جميع البيانات)
pnpm prisma migrate reset
```

## 🗂️ هيكل المشروع

```
FluffNwoof/
├── backend/
│   ├── src/
│   │   ├── config/          # إعدادات قاعدة البيانات والتطبيق
│   │   ├── controllers/     # معالجات الطلبات
│   │   ├── middlewares/     # Auth, validation, error handling
│   │   ├── routes/          # مسارات API
│   │   ├── services/        # منطق الأعمال
│   │   ├── types/           # TypeScript types
│   │   ├── utils/           # دوال مساعدة
│   │   ├── validators/      # التحقق من البيانات
│   │   └── server.ts        # نقطة البداية
│   ├── prisma/
│   │   └── schema.prisma    # مخطط قاعدة البيانات
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── api/             # API client & hooks
│   │   ├── components/      # مكونات قابلة لإعادة الاستخدام
│   │   ├── pages/           # صفحات التطبيق
│   │   ├── store/           # إدارة الحالة
│   │   ├── types/           # TypeScript types
│   │   ├── App.tsx
│   │   └── main.tsx
│   └── package.json
│
└── README.md
```

## 🔐 API Endpoints

### Authentication
- `POST /api/auth/register` - تسجيل مستخدم جديد
- `POST /api/auth/login` - تسجيل الدخول
- `GET /api/auth/profile` - الحصول على بيانات المستخدم
- `POST /api/auth/logout` - تسجيل الخروج

### Owners
- `GET /api/owners` - الحصول على جميع الملاك
- `GET /api/owners/:id` - الحصول على مالك محدد
- `POST /api/owners` - إضافة مالك جديد
- `PUT /api/owners/:id` - تحديث بيانات مالك
- `DELETE /api/owners/:id` - حذف مالك

### Pets
- `GET /api/pets` - الحصول على جميع الحيوانات
- `GET /api/pets/:id` - الحصول على حيوان محدد
- `POST /api/pets` - إضافة حيوان جديد
- `PUT /api/pets/:id` - تحديث بيانات حيوان
- `DELETE /api/pets/:id` - حذف حيوان

### Appointments
- `GET /api/appointments` - الحصول على جميع المواعيد
- `GET /api/appointments/upcoming` - المواعيد القادمة
- `GET /api/appointments/:id` - الحصول على موعد محدد
- `POST /api/appointments` - إنشاء موعد جديد
- `PUT /api/appointments/:id` - تحديث موعد
- `DELETE /api/appointments/:id` - حذف موعد

## 🎨 التصميم

- التصميم بالكامل RTL (من اليمين لليسار) للعربية
- استخدام خط Cairo للعربية
- ألوان مهدئة مناسبة للعيادات البيطرية
- تصميم متجاوب يعمل على جميع الأجهزة

## 🔒 الأمان

- تشفير كلمات المرور باستخدام bcrypt
- JWT للمصادقة
- Validation لجميع المدخلات
- حماية من SQL Injection (عبر Prisma)
- CORS configuration
- Error handling شامل

## 📝 المساهمة

لإضافة مميزات جديدة:

1. أنشئ Service جديد في `backend/src/services/`
2. أنشئ Controller في `backend/src/controllers/`
3. أنشئ Routes في `backend/src/routes/`
4. أضف الـ routes في `backend/src/server.ts`
5. أنشئ مكونات وصفحات Frontend في `frontend/src/`

## 🐛 الأخطاء الشائعة وحلولها

### خطأ في الاتصال بقاعدة البيانات
- تأكد من صحة `DATABASE_URL` في ملف `.env`
- تأكد من أن قاعدة البيانات تعمل
- جرب `pnpm prisma:generate` مجدداً

### خطأ في CORS
- تأكد من أن `FRONTEND_URL` في `.env` يطابق عنوان Frontend
- تأكد من تشغيل Backend على المنفذ الصحيح

### خطأ 401 Unauthorized
- تأكد من وجود token صالح
- جرب تسجيل الدخول مجدداً

## 📞 الدعم

لأي استفسارات أو مشاكل، يرجى فتح Issue في GitHub

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة ISC

---

**تم التطوير بواسطة Claude Code** 🤖
