# 🚀 البدء السريع - FluffNwoof

## خطوات بسيطة للتشغيل

### ⚡ الطريقة السريعة

1. **تأكد من تثبيت Node.js**
   - حمّل من: https://nodejs.org (اختر LTS)
   - افتح Command Prompt واكتب: `node --version`

2. **أنشئ قاعدة بيانات مجانية على Neon**
   - اذهب إلى: https://neon.tech
   - سجل حساب مجاني
   - أنشئ مشروع جديد
   - انسخ Connection String

3. **أنشئ ملف `.env` في مجلد `backend`**
   ```
   DATABASE_URL="الرابط-الذي-نسخته-من-Neon"
   JWT_SECRET="FluffNwoof-2024-Secret"
   PORT=5000
   NODE_ENV=development
   FRONTEND_URL="http://localhost:5173"
   ```

4. **شغّل ملف الإعداد**
   - اضغط دبل كليك على: `setup.bat`
   - انتظر حتى ينتهي التثبيت

5. **جهز قاعدة البيانات**
   ```bash
   cd backend
   npx prisma generate
   npx prisma migrate dev --name init
   ```

6. **أنشئ مستخدم Admin**
   ```bash
   node create-admin.js
   ```

7. **شغّل التطبيق**
   - اضغط دبل كليك على: `start.bat`
   - انتظر 15 ثانية
   - افتح المتصفح على: http://localhost:5173

8. **سجل دخول**
   - Email: `admin@fluffnwoof.com`
   - Password: `admin123`

---

## 🎯 الملفات المهمة

- `setup.bat` ← لتثبيت المكتبات
- `start.bat` ← لتشغيل التطبيق
- `backend/create-admin.js` ← لإنشاء مستخدم admin
- `HOW_TO_RUN.md` ← الدليل التفصيلي

---

## 📋 الأوامر اليدوية (إذا لم تعمل الملفات)

### تثبيت Backend
```bash
cd backend
npm install
```

### تثبيت Frontend
```bash
cd frontend
npm install
```

### إعداد قاعدة البيانات
```bash
cd backend
npx prisma generate
npx prisma migrate dev --name init
```

### إنشاء Admin
```bash
cd backend
node create-admin.js
```

### تشغيل Backend (في نافذة منفصلة)
```bash
cd backend
npm run dev
```

### تشغيل Frontend (في نافذة أخرى)
```bash
cd frontend
npm run dev
```

---

## 🛠️ أدوات مفيدة

### فتح Prisma Studio (لإدارة قاعدة البيانات)
```bash
cd backend
npx prisma studio
```
يفتح على: http://localhost:5555

---

## ❓ مشاكل شائعة

### ❌ Node.js غير موجود
**الحل**: حمّل وثبت من https://nodejs.org

### ❌ خطأ في قاعدة البيانات
**الحل**: تأكد من:
1. ملف `.env` موجود في `backend`
2. `DATABASE_URL` صحيح من Neon
3. الإنترنت متصل

### ❌ Port 5000 مستخدم
**الحل**: غيّر PORT في `.env` إلى 5001

### ❌ صفحة بيضاء
**الحل**:
1. تأكد من تشغيل Backend
2. اضغط F12 في المتصفح وشوف الأخطاء
3. تأكد من ملف `.env` في `frontend`

---

## 🎊 مبروك!

إذا وصلت هنا، يعني التطبيق يعمل! استمتع باستخدام FluffNwoof 🐾

---

**للمساعدة التفصيلية، راجع: [HOW_TO_RUN.md](HOW_TO_RUN.md)**
